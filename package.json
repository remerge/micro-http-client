{
  "name": "micro-http-client",
  "version": "0.0.4",
  "description": "Wraps the Fetch API adding straightforward request and response processing",
  "main": "dist/main.js",
  "files": [
    "dist/main.js",
    "src"
  ],
  "license": "MIT",
  "scripts": {
    "prepublish": "rollup -c",
    "start": "rollup -c --watch",
    "start-test": "jest --watch",
    "test": "eslint . && jest"
  },
  "repository": "remerge/micro-http-client",
  "devDependencies": {
    "babel-preset-env": "^1.4.0",
    "babel-preset-es2015-rollup": "^3.0.0",
    "eslint": "^3.19.0",
    "eslint-config-airbnb-base": "^11.2.0",
    "eslint-plugin-import": "^2.2.0",
    "jest": "^20.0.1",
    "rollup": "^0.41.6",
    "rollup-plugin-babel": "^2.7.1",
    "rollup-watch": "^3.2.2"
  },
  "module": "src/main.js"
}
