{
  "name": "http-client",
  "version": "1.0.0",
  "description": "Wraps the Fetch API adding request, response and error processing",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rollup -c",
    "start": "rollup -c --watch",
    "start-test": "jest --watch",
    "test": "jest"
  },
  "devDependencies": {
    "babel-jest": "^19.0.0",
    "babel-preset-es2015": "^6.24.1",
    "eslint": "^3.19.0",
    "eslint-config-google": "0.7.1",
    "jest": "^20.0.1",
    "rollup": "^0.41.6",
    "rollup-watch": "^3.2.2"
  }
}
